model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Review Python changes in phosphobot with emphasis on type safety, FastAPI patterns, error handling, security, and Pythonic design.
Check for unsafe shell usage, path handling, broad exception swallowing, mutable defaults, blocking work in async code, and weak typing in public interfaces.
Prefer repo-native verification such as targeted pytest, make types, and Ruff-aligned patterns.
"""
